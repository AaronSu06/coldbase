---
phase: 08-postgresql-migration-schema-cleanup
plan: "03"
type: execute
wave: 2
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - server/prisma/migrations/
  - server/index.js
autonomous: false
requirements:
  - DB-01

user_setup:
  - service: neon
    why: "PostgreSQL hosting — both app database and test branch"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console → your project → Connection Details → Connection string (select 'Pooled connection')"
      - name: DIRECT_URL
        source: "Neon Console → your project → Connection Details → Connection string (deselect 'Pooled connection')"
      - name: TEST_DATABASE_URL
        source: "Neon Console → Branches → Create branch (name it 'test') → Connection Details → Pooled connection string"
      - name: TEST_DIRECT_URL
        source: "Neon Console → test branch → Connection Details → direct connection string (no -pooler)"
    dashboard_config:
      - task: "Create a Neon project (if not already created)"
        location: "console.neon.tech → New Project"
      - task: "Create a 'test' branch for test isolation"
        location: "Neon Console → your project → Branches → Create branch"

must_haves:
  truths:
    - "server/prisma/migrations/ contains exactly one migration directory named *_init-postgres"
    - "The init-postgres migration.sql contains no aiSuggestion or draft column definitions"
    - "server/index.js runs prisma migrate deploy via execSync before app.listen"
    - "index.js uses dynamic await import('./app.js') after the migration block"
    - "Server process exits with code 1 and a clear message if migration fails"
  artifacts:
    - path: "server/prisma/migrations/*_init-postgres/migration.sql"
      provides: "PostgreSQL baseline migration"
      contains: "CREATE TABLE"
    - path: "server/index.js"
      provides: "Startup sequence with migration bootstrap"
      contains: "prisma migrate deploy"
  key_links:
    - from: "server/index.js"
      to: "prisma migrate deploy"
      via: "execSync"
      pattern: "execSync.*prisma migrate deploy"
    - from: "server/index.js"
      to: "server/app.js"
      via: "dynamic await import"
      pattern: "await import\\('./app.js'\\)"
---

<objective>
Delete the existing SQLite migrations, have the user generate a fresh PostgreSQL baseline migration via `prisma migrate dev --name init-postgres`, then wire `prisma migrate deploy` into server startup in index.js.

Purpose: Completes the database migration. The schema changes from Plan 01 are already in place; this plan produces the canonical migration file and wires the startup deploy so the server auto-migrates on launch.
Output: Single init-postgres migration in server/prisma/migrations/, updated index.js with fail-fast migration bootstrap
</objective>

<execution_context>
@/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-postgresql-migration-schema-cleanup/08-CONTEXT.md
@.planning/phases/08-postgresql-migration-schema-cleanup/08-RESEARCH.md
@.planning/phases/08-postgresql-migration-schema-cleanup/08-01-SUMMARY.md
@.planning/phases/08-postgresql-migration-schema-cleanup/08-02-SUMMARY.md

<interfaces>
<!-- Current server/index.js (entire file, 5 lines): -->
```javascript
import app from './app.js';
const PORT = 3001;
app.listen(PORT, () => {
  console.log(`[Reach server] Listening on http://localhost:${PORT}`);
});
```

<!-- server/package.json type field: "module" — top-level await is valid -->
<!-- __dirname is not available in ESM — must be derived from import.meta.url -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete SQLite migrations + wire startup migration in index.js</name>
  <files>server/index.js</files>
  <action>
Step 1 — Delete all existing SQLite migration directories:
```bash
rm -rf server/prisma/migrations/
```
This removes all 6 existing migration directories and migration_lock.toml. The directory will be recreated by prisma migrate dev in the checkpoint.

Step 2 — Rewrite server/index.js with the fail-fast migration bootstrap pattern. The server has "type": "module" in package.json, so top-level await is valid. Dynamic import of app.js ensures the app module loads AFTER migration completes (critical: ES module static imports are hoisted and would load before execSync runs).

New index.js content:
```javascript
import { execSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';
import { dirname } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));

try {
  execSync('npx prisma migrate deploy', {
    cwd: __dirname,
    stdio: 'inherit',
    env: { ...process.env },
  });
} catch (err) {
  console.error(`DB migration failed: ${err.message}. Check DATABASE_URL and retry.`);
  process.exit(1);
}

// Dynamic import ensures app module (and Prisma client) loads after migration completes
const { default: app } = await import('./app.js');
const PORT = process.env.PORT || 3001;
app.listen(PORT, () => {
  console.log(`[Reach server] Listening on http://localhost:${PORT}`);
});
```

Note: Using process.env.PORT || 3001 so the server respects PORT env var if set (useful for deployment). This is Claude's discretion — no locked decision on port handling.
  </action>
  <verify>
    <automated>grep -n "prisma migrate deploy" /Users/aaron/Documents/GitHub/reach/server/index.js && grep -n "await import" /Users/aaron/Documents/GitHub/reach/server/index.js && grep -n "process.exit(1)" /Users/aaron/Documents/GitHub/reach/server/index.js && ! test -d /Users/aaron/Documents/GitHub/reach/server/prisma/migrations</automated>
  </verify>
  <done>index.js contains execSync prisma migrate deploy, dynamic await import('./app.js'), and process.exit(1) on failure; migrations/ directory is deleted</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Generate PostgreSQL baseline migration</name>
  <files>server/prisma/migrations/</files>
  <action>
Human action required: Run `prisma migrate dev --name init-postgres` against Neon to generate the canonical baseline migration. Claude cannot execute this step because it requires a live Neon connection with valid DATABASE_URL / DIRECT_URL env vars.

Instructions:
1. Ensure server/.env has DATABASE_URL (pooled) and DIRECT_URL (direct) set to your Neon connection strings.
   See user_setup frontmatter for where to find these values in the Neon Console.

2. From the server/ directory, run:
   ```
   cd server && npx prisma migrate dev --name init-postgres
   ```
   prisma migrate dev uses directUrl automatically for the schema engine (pgbouncer bypass).

3. Verify the migration was created:
   ```
   ls server/prisma/migrations/
   ```
   You should see exactly one directory named like `20260317123456_init-postgres`.

4. Confirm no aiSuggestion or draft columns in the generated SQL:
   ```
   cat server/prisma/migrations/*/migration.sql | grep -i "aiSuggestion\|draft"
   ```
   This should return no output.

5. Optionally run the test suite (requires TEST_DATABASE_URL and TEST_DIRECT_URL in env):
   ```
   cd server && node --test outreach.test.js
   ```
  </action>
  <verify>
    <automated>MISSING — requires live Neon connection; user must confirm migration file exists at server/prisma/migrations/*_init-postgres/migration.sql</automated>
  </verify>
  <done>server/prisma/migrations/ contains exactly one directory (*_init-postgres); migration.sql has no aiSuggestion or draft column definitions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify server starts against PostgreSQL</name>
  <files>server/index.js</files>
  <action>
Human verification required: Confirm server starts with PostgreSQL DATABASE_URL and that prisma migrate deploy runs at startup without error.

Steps:
1. Ensure server/.env has DATABASE_URL and DIRECT_URL set to Neon values.
2. Run: `cd server && node index.js`
3. Expected output: Prisma migrate deploy output ("No pending migrations" or "Applied 1 migration"), then "[Reach server] Listening on http://localhost:3001"
4. Test an endpoint: `curl -s -H "x-reach-secret: YOUR_SECRET" http://localhost:3001/api/outreach`
   Should return JSON (empty array or records), not an error.
  </action>
  <verify>
    <automated>MISSING — requires live Neon connection; user must confirm server startup logs show migrate deploy output and server responds to API requests</automated>
  </verify>
  <done>Server starts successfully with PostgreSQL; prisma migrate deploy runs on startup; API endpoints return correct responses</done>
</task>

</tasks>

<verification>
Automated (after Task 1):
- `grep "prisma migrate deploy" server/index.js` — matches
- `grep "await import" server/index.js` — matches
- `grep "process.exit(1)" server/index.js` — matches

After Task 2 checkpoint:
- `ls server/prisma/migrations/` — shows exactly one directory
- `cat server/prisma/migrations/*/migration.sql | grep "aiSuggestion\|draft"` — no output

After Task 3 checkpoint:
- Server starts and responds to API requests against PostgreSQL
</verification>

<success_criteria>
1. server/prisma/migrations/ contains exactly one migration directory (*_init-postgres)
2. migration.sql contains no aiSuggestion or draft column definitions
3. index.js runs prisma migrate deploy via execSync before app.listen, with process.exit(1) on failure
4. Server starts successfully with DATABASE_URL pointing to PostgreSQL
5. All API endpoints return correct responses against PostgreSQL
</success_criteria>

<output>
After completion, create `.planning/phases/08-postgresql-migration-schema-cleanup/08-03-SUMMARY.md`
</output>
