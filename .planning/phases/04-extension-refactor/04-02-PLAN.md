---
phase: 04-extension-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - extension/background.js
  - extension/auth.js
  - extension/api-client.js
  - extension/reply-checker.js
autonomous: true
requirements:
  - EXT-01
  - EXT-03
  - EXT-04

must_haves:
  truths:
    - "extension/auth.js exists and exports getAuthToken()"
    - "extension/api-client.js exists and exports apiFetch, apiFetchRetry, getFullMessage, plus server call helpers"
    - "extension/reply-checker.js exists and exports checkReplies(), trackLatestSent()"
    - "background.js is an orchestrator only — imports from modules, owns chrome.* listeners and message handler, contains no business logic"
    - "No raw console.log/warn/error calls remain in background.js or the three new modules"
    - "No silent catch {} or .catch(() => {}) blocks remain in background.js files"
    - "Auth failures log + rethrow; server call failures log + return { ok: false, error }"
  artifacts:
    - path: "extension/auth.js"
      provides: "getAuthToken() — OAuth token management"
      exports: ["getAuthToken"]
    - path: "extension/api-client.js"
      provides: "Gmail API transport + server API calls"
      exports: ["apiFetch", "apiFetchRetry", "getFullMessage", "postOutreach", "postTrackingPixel", "fetchOutreach", "serverFetch"]
    - path: "extension/reply-checker.js"
      provides: "Reply detection logic + parsing helpers + trackLatestSent"
      exports: ["checkReplies", "trackLatestSent"]
    - path: "extension/background.js"
      provides: "Orchestrator — chrome event listeners + message dispatcher"
  key_links:
    - from: "extension/background.js"
      to: "extension/auth.js"
      via: "ES module import"
      pattern: "import.*getAuthToken.*auth.js"
    - from: "extension/background.js"
      to: "extension/api-client.js"
      via: "ES module import"
      pattern: "import.*api-client.js"
    - from: "extension/background.js"
      to: "extension/reply-checker.js"
      via: "ES module import"
      pattern: "import.*reply-checker.js"
    - from: "extension/reply-checker.js"
      to: "extension/auth.js"
      via: "ES module import (for token during reply scan)"
      pattern: "import.*auth.js"
    - from: "extension/reply-checker.js"
      to: "extension/api-client.js"
      via: "ES module import (for Gmail API fetch)"
      pattern: "import.*api-client.js"
---

<objective>
Split background.js (563 lines) into three focused ES modules — auth.js, api-client.js, reply-checker.js — then reduce background.js to a lean orchestrator that imports them. Replace all raw console.log calls with structured logger output. Fix all silent catch blocks per the EXT-04 escalation policy.

Purpose: EXT-01 structural requirement — background.js becomes the message dispatcher only; business logic lives in its domain module.
Output: Three new ES module files + slimmed-down background.js orchestrator.
</objective>

<execution_context>
@/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-extension-refactor/04-CONTEXT.md
@.planning/phases/04-extension-refactor/04-RESEARCH.md
@.planning/phases/04-extension-refactor/04-01-SUMMARY.md

<interfaces>
<!-- Logger — from Plan 01 output -->
From extension/logger.js:
```javascript
export { makeLogger as logger };
// Usage: const log = logger('auth');
```

<!-- config.js exports -->
From extension/config.js:
```javascript
export const SERVER_URL = 'http://localhost:3001/api';
export const DASH_URL = 'http://localhost:5173';
export const REACH_SECRET = 'CHANGE_ME';
export const DEBUG = true;
```

<!-- classifier.js — imported by background.js, stays there -->
From extension/classifier.js:
```javascript
export { isColdOutreach, countKeywordMatches, extractCompanyFromEmail,
         extractCompanyFromText, fetchClearbitCompany, extractFirstName, isGenericDomain }
```

<!-- Function-to-module map (from research) -->
auth.js:
  - getAuthToken(interactive = false) — lines 9–18 of background.js

api-client.js:
  - apiFetch(url, token) — lines 22–34
  - getFullMessage(token, messageId) — lines 36–38
  - apiFetchRetry(url, token) — lines 42–51
  - PLUS: server API call functions (postOutreach, postTrackingPixel, fetchOutreach, serverFetch)
    These are the inline fetch() calls inside the chrome.runtime.onMessage handler branches
    that post to SERVER_URL. Extract them to named functions in api-client.js.

reply-checker.js:
  - extractHeader(message, name) — lines 55–58
  - decodeBase64Url(data) — lines 60–73 (has silent catch — fix per EXT-04)
  - findPart(parts, mimeType) — lines 75–84
  - extractBody(message) — lines 86–98
  - extractEmailAddress(str) — lines 100–103
  - shortFrom(from) — lines 105–109
  - stripQuotedText(text) — lines 111–114
  - normalizeForMatch(str) — lines 116–123
  - buildConversationPreview(messages) — lines 125–141
  - trackLatestSent() + _trackLatestSent() — lines 147–302
  - checkReplies() — lines 306–374 (has silent catch at line 370 — fix per EXT-04)
  - scanInProgress (module-level let) stays in reply-checker.js

background.js (stays):
  - All chrome.* event listeners
  - chrome.runtime.onMessage dispatcher
  - Imports from all three new modules
  - No business logic
</interfaces>

<!-- Silent catch locations to fix (EXT-04) -->
<!--
background.js line 70 (decodeBase64Url, moves to reply-checker.js):
  catch { return ''; }  →  catch (e) { log.debug('decodeBase64Url: malformed input', e?.message); return ''; }

background.js line 370 (checkReplies per-thread loop, moves to reply-checker.js):
  catch {}  →  catch (e) { log.error('Reply check failed for thread', record.threadId, e); }

background.js line 449 (GET_STATS .catch, stays in background.js or moves to api-client.js):
  .catch(() => sendResponse({ ok: false }))
  →  .catch((e) => { log.error('GET_STATS fetch failed:', e); sendResponse({ ok: false }); })

background.js line 492 (GET_RECENT .catch, stays in background.js or moves to api-client.js):
  .catch(() => sendResponse({ ok: false }))
  →  .catch((e) => { log.error('GET_RECENT fetch failed:', e); sendResponse({ ok: false }); })
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth.js and api-client.js</name>
  <files>extension/auth.js, extension/api-client.js</files>
  <action>
    Read extension/background.js fully before writing. Then create two new ES module files.

    **extension/auth.js:**
    - Import logger from './logger.js'
    - Create `const log = logger('auth')`
    - Export `getAuthToken(interactive = false)` — exact logic from background.js lines 9–18
    - Auth failures: log.error then rethrow (per EXT-04 auth escalation policy)
    - No other functions

    **extension/api-client.js:**
    - Import { logger } from './logger.js'
    - Import { SERVER_URL, REACH_SECRET } from './config.js'
    - Create `const log = logger('api-client')`
    - Export these functions (move from background.js):
      - `apiFetch(url, token)` — lines 22–34. On 401: log.warn token expired before throwing.
      - `getFullMessage(token, messageId)` — lines 36–38
      - `apiFetchRetry(url, token)` — lines 42–51. Replace console.warn with log.warn.
    - Also export server API helpers by extracting the inline fetch() calls from the
      onMessage handler. Name them descriptively:
      - `serverFetch(path, options)` — base helper that attaches REACH_SECRET header and
        calls SERVER_URL. Extract the repeated pattern of `fetch(\`${SERVER}/...\`, { headers: { 'x-reach-secret': REACH_SECRET } })`.
      - `postOutreach(payload)` — wraps the POST to /api/outreach
      - `postTrackingPixel(payload)` — wraps the POST to tracking pixel endpoint (if present)
      - `fetchOutreach(params)` — wraps GET /api/outreach (used by GET_STATS, GET_RECENT branches)
      Use your judgment on exact function signatures — extract what makes background.js cleaner.
      Not every server call needs its own named function; serverFetch() as a base is sufficient
      if the callers differ too much to abstract further. Prioritize clarity over perfect DRY.

    Replace all console.log/warn/error with log.debug/info/warn/error appropriately.
    No silent catches — if apiFetchRetry's catch swallows anything, add log.error.
  </action>
  <verify>
    <automated>node --input-type=module &lt;&lt;'EOF'
import { getAuthToken } from './extension/auth.js';
import { apiFetch, apiFetchRetry, getFullMessage } from './extension/api-client.js';
if (typeof getAuthToken !== 'function') throw new Error('getAuthToken missing');
if (typeof apiFetch !== 'function') throw new Error('apiFetch missing');
if (typeof apiFetchRetry !== 'function') throw new Error('apiFetchRetry missing');
if (typeof getFullMessage !== 'function') throw new Error('getFullMessage missing');
console.log('OK: auth.js and api-client.js exports present');
EOF</automated>
  </verify>
  <done>
    - extension/auth.js exports getAuthToken
    - extension/api-client.js exports apiFetch, apiFetchRetry, getFullMessage, serverFetch (and any additional server helpers extracted)
    - No console.log/warn/error in either file (all replaced by log.*)
    - No silent catches in either file
  </done>
</task>

<task type="auto">
  <name>Task 2: Create reply-checker.js and rewrite background.js as orchestrator</name>
  <files>extension/reply-checker.js, extension/background.js</files>
  <action>
    **extension/reply-checker.js:**
    - Import { logger } from './logger.js'
    - Import { getAuthToken } from './auth.js'
    - Import { apiFetch, apiFetchRetry, getFullMessage, serverFetch } from './api-client.js' (import what's needed)
    - Import { SERVER_URL, REACH_SECRET } from './config.js' if needed by trackLatestSent
    - Create `const log = logger('reply-checker')`
    - Move ALL parsing helpers: extractHeader, decodeBase64Url, findPart, extractBody,
      extractEmailAddress, shortFrom, stripQuotedText, normalizeForMatch, buildConversationPreview
    - Move trackLatestSent + _trackLatestSent (lines 147–302)
    - Move checkReplies (lines 306–374)
    - Move `let scanInProgress = false;` module-level flag
    - Export: checkReplies, trackLatestSent (these are the only functions background.js calls)
    - Fix EXT-04 silent catches:
      - decodeBase64Url: `catch (e) { log.debug('decodeBase64Url: malformed input', e?.message); return ''; }`
      - checkReplies per-thread: `catch (e) { log.error('Reply check failed for thread', record.threadId, e); }`
    - Replace all console.log/warn/error with log.debug/info/warn/error

    **extension/background.js (rewrite as orchestrator):**
    - Keep imports at top:
      ```javascript
      import { isColdOutreach, countKeywordMatches, extractCompanyFromEmail,
               extractCompanyFromText, fetchClearbitCompany, extractFirstName, isGenericDomain } from './classifier.js';
      import { SERVER_URL, DASH_URL, REACH_SECRET } from './config.js';
      import { logger } from './logger.js';
      import { getAuthToken } from './auth.js';
      import { apiFetch, apiFetchRetry, getFullMessage, serverFetch } from './api-client.js';
      import { checkReplies, trackLatestSent } from './reply-checker.js';
      ```
      (Adjust api-client.js imports to match what was actually exported in Task 1)
    - `const log = logger('background');`
    - Keep ALL chrome.* event listeners (storage.onChanged, runtime.onInstalled, alarms.onAlarm, action.onClicked)
    - Keep the FULL chrome.runtime.onMessage handler with ALL branches intact —
      message surface stays visible in one place. Each branch now calls the imported module functions.
    - Fix EXT-04 in onMessage branches:
      - GET_STATS .catch: `(e) => { log.error('GET_STATS failed:', e); sendResponse({ ok: false }); }`
      - GET_RECENT .catch: `(e) => { log.error('GET_RECENT failed:', e); sendResponse({ ok: false }); }`
    - Preserve ALL `return true;` statements in async message handler branches —
      these keep the message channel open. NEVER move them into modules.
    - Replace any remaining console.log/warn/error with log.*
    - CRITICAL: The GMAIL_API constant and SERVER constant used internally should stay or
      move to api-client.js (wherever the fetch calls live). Do not duplicate them.

    After rewrite, background.js should contain only:
    - imports
    - constants (GMAIL_API if still needed at top level)
    - const log = logger('background')
    - chrome.* event listener registrations
    - chrome.runtime.onMessage handler with branches delegating to modules
    - NO function definitions (all moved to modules)
  </action>
  <verify>
    <automated>node --input-type=module &lt;&lt;'EOF'
import { checkReplies, trackLatestSent } from './extension/reply-checker.js';
if (typeof checkReplies !== 'function') throw new Error('checkReplies missing');
if (typeof trackLatestSent !== 'function') throw new Error('trackLatestSent missing');

// Verify no silent catches remain in background-side files
import { execSync } from 'child_process';
const result = execSync('grep -n "catch {" extension/background.js extension/auth.js extension/api-client.js extension/reply-checker.js 2>/dev/null || true').toString();
if (result.trim()) { console.error('Silent catches found:', result); process.exit(1); }

// Verify no raw console.log in the four files
const logs = execSync('grep -n "console\\.log\\|console\\.warn\\|console\\.error" extension/background.js extension/auth.js extension/api-client.js extension/reply-checker.js 2>/dev/null || true').toString();
if (logs.trim()) { console.error('Raw console calls found:', logs); process.exit(1); }

console.log('OK: reply-checker.js exports present, no silent catches, no raw console calls');
EOF</automated>
  </verify>
  <done>
    - extension/reply-checker.js exports checkReplies and trackLatestSent
    - extension/background.js imports from auth.js, api-client.js, reply-checker.js
    - background.js contains no function definitions (all moved out)
    - All chrome.* event listeners and onMessage handler remain in background.js
    - All `return true;` statements preserved in onMessage async branches
    - No console.log/warn/error in any of the four background-side files
    - No silent catch {} blocks in any of the four files
    - decodeBase64Url catch logs at debug level
    - checkReplies per-thread catch logs at error level with threadId
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn "catch {" extension/background.js extension/auth.js extension/api-client.js extension/reply-checker.js` returns zero results
2. `grep -rn "\.catch(() =>" extension/background.js extension/auth.js extension/api-client.js extension/reply-checker.js` returns zero results
3. `grep -rn "console\.log\|console\.warn\|console\.error" extension/background.js extension/auth.js extension/api-client.js extension/reply-checker.js` returns zero results
4. All four files exist: background.js, auth.js, api-client.js, reply-checker.js
5. background.js has no function definitions beyond the arrow functions inside chrome.* handlers
</verification>

<success_criteria>
- EXT-01: background.js is orchestrator-only; auth/API/reply logic in separate modules
- EXT-03: No raw console.log in background-side files; all replaced with log.*
- EXT-04: No silent catch {} or .catch(() => {}) in background-side files
- Import chain works: background.js → auth.js, api-client.js, reply-checker.js → auth.js + api-client.js (no circular deps)
- All chrome.* message handler `return true` statements preserved
</success_criteria>

<output>
After completion, create `.planning/phases/04-extension-refactor/04-02-SUMMARY.md`
</output>
