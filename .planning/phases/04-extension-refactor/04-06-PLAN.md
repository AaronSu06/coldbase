---
phase: 04-extension-refactor
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/compose-widget.js
autonomous: true
gap_closure: true
requirements:
  - EXT-04

must_haves:
  truths:
    - "No silent catch {} blocks remain in extension/compose-widget.js"
    - "chrome.storage.local.set() errors in the tracking-mode toggle handler are logged via log.error"
  artifacts:
    - path: "extension/compose-widget.js"
      provides: "Compose panel UI widget with explicit error handling"
      contains: "catch (e) { log.error"
  key_links:
    - from: "extension/compose-widget.js line 678 catch block"
      to: "window.ReachLogger('compose-widget') — log const declared at line 6"
      via: "log.error call"
      pattern: "log\\.error\\('Failed to persist trackingDefault"
---

<objective>
Close the single remaining EXT-04 violation: a silent `catch (_) {}` on line 678 of `extension/compose-widget.js` that swallows `chrome.storage.local.set()` errors inside the tracking-mode toggle button handler.

Purpose: EXT-04 requires all catch blocks log the error at minimum. This is the only outstanding gap blocking full EXT-04 satisfaction. The `log` constant is already in scope at line 6 — the fix is a one-line substitution.
Output: `extension/compose-widget.js` with the silent catch replaced by `catch (e) { log.error('Failed to persist trackingDefault:', e); }`
</objective>

<execution_context>
@/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-extension-refactor/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace silent catch on line 678 of compose-widget.js</name>
  <files>extension/compose-widget.js</files>
  <action>
    On line 678, replace:
      `try { chrome.storage.local.set({ trackingDefault: mode }); } catch (_) {}`
    with:
      `try { chrome.storage.local.set({ trackingDefault: mode }); } catch (e) { log.error('Failed to persist trackingDefault:', e); }`

    The `log` constant is already declared at line 6: `const log = window.ReachLogger('compose-widget');`
    No other changes to this file. Do not reformat, reorder, or adjust surrounding lines.
  </action>
  <verify>
    <automated>grep -n "catch (_) {}" /Users/aaron/Documents/GitHub/reach/extension/compose-widget.js | wc -l | grep -q "^0$" && echo "PASS: no silent catch (_) {} found" && grep -n "Failed to persist trackingDefault" /Users/aaron/Documents/GitHub/reach/extension/compose-widget.js && echo "PASS: log.error call present"</automated>
  </verify>
  <done>
    `grep -n "catch (_) {}" extension/compose-widget.js` returns zero results.
    `grep -n "Failed to persist trackingDefault" extension/compose-widget.js` returns one result at the former line 678.
  </done>
</task>

</tasks>

<verification>
Run both greps after the edit:

```bash
# Must return 0
grep -c "catch (_) {}" extension/compose-widget.js

# Must return 1
grep -c "Failed to persist trackingDefault" extension/compose-widget.js
```

No other verification needed — the human smoke test was already completed and approved on 2026-03-16. This one-line change adds logging only; no behavioral change, no new imports, no manifest changes.
</verification>

<success_criteria>
- `extension/compose-widget.js` contains zero `catch (_) {}` occurrences
- `extension/compose-widget.js` contains `log.error('Failed to persist trackingDefault:', e)` at the former line 678 location
- EXT-04 is fully satisfied: no silent catch blocks remain anywhere in the extension
- Phase 4 verification score advances from 21/22 to 22/22
</success_criteria>

<output>
After completion, create `.planning/phases/04-extension-refactor/04-06-SUMMARY.md`
</output>
