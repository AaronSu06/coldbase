---
phase: 04-extension-refactor
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified: []
autonomous: false
requirements:
  - EXT-01
  - EXT-02
  - EXT-03
  - EXT-04

must_haves:
  truths:
    - "grep for silent catches returns zero results across all extension JS files"
    - "grep for raw console.log returns zero results across all extension JS files"
    - "All seven new/modified extension files exist"
    - "Extension loads without errors in Chrome"
    - "Gmail compose widget appears when opening a new compose window"
    - "Sending a test email triggers the tracking scan"
  artifacts:
    - path: "extension/logger.js"
      provides: "Structured logging factory"
    - path: "extension/auth.js"
      provides: "OAuth token management"
    - path: "extension/api-client.js"
      provides: "Gmail API + server API transport"
    - path: "extension/reply-checker.js"
      provides: "Reply detection and parsing"
    - path: "extension/email-detector.js"
      provides: "Send detection and DOM observation"
    - path: "extension/compose-widget.js"
      provides: "Compose panel UI widget"
    - path: "extension/tracking.js"
      provides: "Tracking pixel injection"
  key_links:
    - from: "extension/manifest.json"
      to: "all seven new files"
      via: "content_scripts js array and service worker declaration"
      pattern: "logger.js|email-detector.js|compose-widget.js|tracking.js"
---

<objective>
Final automated verification sweep and human smoke test. Run grep checks to confirm all EXT-04 silent catches and EXT-03 raw console calls are eliminated. Human confirms the extension loads and functions correctly in Chrome after the refactor.

Purpose: Validate the complete Phase 4 refactor before marking it done. No behavior change should have occurred — this checkpoint confirms that.
Output: Phase 4 marked complete; smoke test approved.
</objective>

<execution_context>
@/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-extension-refactor/04-02-SUMMARY.md
@.planning/phases/04-extension-refactor/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated verification sweep</name>
  <files></files>
  <action>
    Run the following grep checks across all extension JS files. Report findings for each check.
    If any check fails (returns results), list exactly which files and lines are affected.
    Do NOT proceed to the checkpoint task until all checks pass.

    Check 1 — No silent bare catches:
    `grep -rn "catch {" extension/*.js`
    Expected: zero results

    Check 2 — No silent arrow catches:
    `grep -rn "\.catch(() =>" extension/*.js`
    Expected: zero results

    Check 3 — No raw console.log calls:
    `grep -rn "console\.log(" extension/*.js`
    Expected: zero results

    Check 4 — No raw console.warn calls:
    `grep -rn "console\.warn(" extension/*.js`
    Expected: zero results (logger uses log.warn internally; this checks for accidental raw calls)
    Note: logger.js itself will have console.debug/log/error inside makeLogger — that is expected.
    The grep should exclude logger.js: `grep -rn "console\.warn(" extension/*.js | grep -v "logger.js"`
    Expected: zero results from non-logger files

    Check 5 — All seven new files exist:
    `ls extension/logger.js extension/auth.js extension/api-client.js extension/reply-checker.js extension/email-detector.js extension/compose-widget.js extension/tracking.js`
    Expected: all seven listed without error

    Check 6 — No ES module imports in content-side modules:
    `grep -rn "^import " extension/email-detector.js extension/compose-widget.js extension/tracking.js extension/content.js`
    Expected: zero results

    Check 7 — Manifest load order:
    `node -e "const m = require('./extension/manifest.json'); const js = m.content_scripts.find(c => c.matches.includes('https://mail.google.com/*')).js; console.log(js.join(', '));"`
    Expected output contains: logger.js, email-detector.js, compose-widget.js, tracking.js, content.js, sidebar.js in that order

    Check 8 — background.js imports all three new modules:
    `grep "import.*from" extension/background.js`
    Expected: lines importing from ./auth.js, ./api-client.js, ./reply-checker.js, ./logger.js

    Report all results. If any check fails, fix the issue before proceeding.
  </action>
  <verify>
    <automated>
grep -rn "catch {" extension/*.js | grep -v "Binary" && echo "FAIL: silent catches found" || echo "PASS: no silent catches"
grep -rn "\.catch(() =>" extension/*.js | grep -v "Binary" && echo "FAIL: silent arrow catches found" || echo "PASS: no silent arrow catches"
grep -rn "console\.log(" extension/*.js | grep -v "logger.js" | grep -v "Binary" && echo "FAIL: raw console.log found" || echo "PASS: no raw console.log"
ls extension/logger.js extension/auth.js extension/api-client.js extension/reply-checker.js extension/email-detector.js extension/compose-widget.js extension/tracking.js && echo "PASS: all seven files exist"
    </automated>
  </verify>
  <done>All eight checks pass with zero unexpected results</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 4 extension refactor complete:
    - background.js split into auth.js, api-client.js, reply-checker.js (EXT-01)
    - content.js split into email-detector.js, compose-widget.js, tracking.js (EXT-02)
    - extension/logger.js created with debug/info/error levels and [Reach/module] prefix (EXT-03)
    - All raw console.log calls replaced with structured logger output (EXT-03)
    - All silent catch {} and .catch(() => {}) blocks replaced with error logging (EXT-04)
    - manifest.json updated with correct load order
  </what-built>
  <how-to-verify>
    1. Open Chrome and go to chrome://extensions
    2. Click "Load unpacked" and select the extension/ directory (or click the reload button if already loaded)
    3. Confirm extension loads with no errors shown in the extension card
    4. Open DevTools for the extension (click "Service Worker" link) — confirm no import errors in console
    5. Navigate to https://mail.google.com
    6. Open DevTools in the Gmail tab (F12), go to Console tab
    7. Confirm console shows "[Reach/content] Content script loaded." (or similar prefixed message)
    8. Confirm no "[Reach] Content script loaded." (old unprefixed format)
    9. Click "Compose" — confirm the Reach compose widget appears on the right side of the compose window
    10. Send a test email to yourself — confirm no errors in DevTools console

    Optional (to verify EXT-03 debug suppression):
    - In extension/logger.js, temporarily change `const DEBUG = true` to `const DEBUG = false`
    - Reload extension (chrome://extensions → reload button)
    - Open Gmail — confirm only [Reach/*/error] messages appear; no debug/info logs
    - Revert logger.js back to `const DEBUG = true` after verifying
  </how-to-verify>
  <resume-signal>Type "approved" when smoke test passes, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete when:
1. All automated grep checks pass (Task 1)
2. Human confirms extension loads without errors (checkpoint)
3. Human confirms compose widget appears in Gmail (checkpoint)
4. Human confirms send tracking still fires (checkpoint)
</verification>

<success_criteria>
- EXT-01: background.js is orchestrator-only (verified by file structure + import grep)
- EXT-02: content.js is orchestrator-only (verified by file structure + namespace grep)
- EXT-03: All console.log replaced with structured logger (verified by grep + DevTools inspection)
- EXT-04: No silent catches (verified by grep returning zero results)
- Extension functional: compose widget loads, tracking fires on send (verified by human smoke test)
</success_criteria>

<output>
After human approval, create `.planning/phases/04-extension-refactor/04-04-SUMMARY.md`
</output>
