---
phase: 09-observability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [server/.env.test]
autonomous: false
gap_closure: true
requirements: [OBS-02]

must_haves:
  truths:
    - "All 5 observability tests pass (3 OBS-01, 2 OBS-02) with exit code 0"
    - "GET /health returns HTTP 200 in the test environment — confirming DB connectivity"
    - "server/.env.test has TEST_DATABASE_URL and TEST_DIRECT_URL pointing to the Neon test branch"
  artifacts:
    - path: "server/.env.test"
      provides: "Test environment PostgreSQL credentials"
      contains: "TEST_DATABASE_URL, TEST_DIRECT_URL"
  key_links:
    - from: "server/observability.test.js"
      to: "server/.env.test"
      via: "dotenv/config loads .env.test at test startup"
      pattern: "TEST_DATABASE_URL"
    - from: "server/app.js GET /health"
      to: "prisma.$queryRaw SELECT 1"
      via: "DATABASE_URL env var read by Prisma client"
      pattern: "TEST_DATABASE_URL"
---

<objective>
Populate server/.env.test with PostgreSQL test credentials so that OBS-02 health-check tests can reach the Neon test branch.

Purpose: The implementation code for OBS-02 (GET /health) is complete and correct. The two failing OBS-02 tests return 503 because server/.env.test still contains a stale SQLite DATABASE_URL and lacks TEST_DATABASE_URL / TEST_DIRECT_URL. Once credentials are added, the tests pass without any code changes.

Output: server/.env.test updated with valid Neon test branch URLs; all 5 observability tests GREEN.
</objective>

<execution_context>
@/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-observability/09-CONTEXT.md
@.planning/phases/09-observability/09-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Add Neon test credentials to server/.env.test</name>
  <files>server/.env.test</files>
  <action>
    Human must update server/.env.test with Neon test branch PostgreSQL credentials.
    Claude cannot perform this step — real database connection strings are required and only the user has them.

    server/.env.test currently contains:
      REACH_SECRET=test-secret
      DATABASE_URL=file:./test.db

    It must be updated to:
      REACH_SECRET=test-secret
      TEST_DATABASE_URL=&lt;neon-test-branch-pooled-connection-string&gt;
      TEST_DIRECT_URL=&lt;neon-test-branch-direct-connection-string&gt;

    The stale DATABASE_URL=file:./test.db line must be removed — it causes the Prisma client
    to use SQLite in the test environment, which is incompatible with the PostgreSQL schema.

    The pooled URL contains -pooler in the hostname. The direct URL does not.
    Both point to the Neon test branch (not the main/production branch).

    Where to find them: Neon dashboard → your project → Branches → select test branch →
    Connection Details → "Connection string" format.
  </action>
  <verify>MISSING — human confirms completion via resume-signal below</verify>
  <done>server/.env.test contains TEST_DATABASE_URL and TEST_DIRECT_URL; DATABASE_URL=file:./test.db is removed</done>
  <resume-signal>Type "done" when server/.env.test has been updated with both TEST_DATABASE_URL and TEST_DIRECT_URL values.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify all observability tests pass</name>
  <files>server/.env.test</files>
  <action>
    Run the observability test suite to confirm all 5 tests (3 OBS-01, 2 OBS-02) pass now that Neon test credentials are populated in server/.env.test.

    The test file loads env vars from server/.env.test at startup via the existing dotenv pattern. When TEST_DATABASE_URL is populated, the Prisma client in the /health handler can complete the SELECT 1 query and return 200.

    If any test still fails after env vars are set:
    - Confirm DATABASE_URL=file:./test.db was removed from .env.test (stale SQLite path overrides TEST_DATABASE_URL)
    - Confirm the Neon test branch is active and not suspended in the Neon dashboard
    - Confirm pooled vs direct URLs were not swapped (TEST_DATABASE_URL = pooler endpoint, contains -pooler in hostname)

    Also run the full server test suite to confirm no regressions in outreach, tracking, or analytics tests.
  </action>
  <verify>
    <automated>cd /Users/aaron/Documents/GitHub/reach/server && node --test observability.test.js</automated>
  </verify>
  <done>
    All 5 observability tests pass:
    - OBS-01 test 1: request produces structured JSON log line — PASS
    - OBS-01 test 2: log fields match expected shape — PASS
    - OBS-01 test 3: x-reach-secret not in log output — PASS
    - OBS-02 test 1: GET /health returns HTTP 200 with no auth header — PASS
    - OBS-02 test 2: /health response body has status, uptime, version, dbLatencyMs — PASS

    Full suite: node --test --test-concurrency=1 server/*.test.js exits 0 with no failures.
  </done>
</task>

</tasks>

<verification>
Run after Task 2 completes:

```bash
cd /Users/aaron/Documents/GitHub/reach/server && node --test observability.test.js
```

Expected: 5 tests pass, 0 fail, exit code 0.

Full regression check:
```bash
cd /Users/aaron/Documents/GitHub/reach && node --test --test-concurrency=1 server/*.test.js
```

Expected: all tests pass, exit code 0.
</verification>

<success_criteria>
- server/.env.test contains TEST_DATABASE_URL and TEST_DIRECT_URL with valid Neon test branch values
- server/.env.test no longer contains DATABASE_URL=file:./test.db
- node --test server/observability.test.js exits 0 with 5/5 tests passing
- OBS-02 requirement is fully satisfied: GET /health returns 200 confirmed by passing tests
- Full server test suite (server/*.test.js) passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-observability/09-03-SUMMARY.md`
</output>
