# Project Rules

## GSD Workflow

**Do not commit markdown-only changes during GSD planning phases.**

When GSD writes or updates `.md` files (plans, verification docs, phase notes, etc.), skip the intermediate commit. These files are being written and revised locally — committing every markdown edit creates noise in git history. Only commit markdown files when they're bundled with actual code changes or when explicitly asked.
